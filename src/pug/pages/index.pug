extends ../layout/main

block variables
  - var title = 'webpack template';
  - var description = 'webpack template';
  - var pageClass = 'index-page';

block content
  h1 Hello :)
  a(href='https://github.com/maxnnn1900/webpack-template')
    img(src='/assets/img/logo.png' alt='webpack template') 
  .mixins
    +sample('sample mixin', 'mixin1 description')
    +sample('sample mixin', 'mixin2 description')
    +sample('sample mixin', 'mixin3 description')
  #components 
    example-component

block script 
  script(type="text/javascript"). 
    const foo = true;
    let bar = function() {};
    if (foo) {
      bar(1+5);
    }